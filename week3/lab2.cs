using System;
namespace next {
	class HelloNext {
		static void Main(String[] args) {
			String cmd, name;
			double width, height, area;

			//fix and complete code
			Console.Write("이름? ");
			cmd = Console.ReadLine();
			name = "";

			//input first number
			Console.Write("밑변의 길이 (cm): ");
			cmd = Console.ReadLine();
			width = Convert.ToDouble(cmd);

			//input second number
			height = area = 0;

			//print result
			Console.WriteLine("삼각형의 무게는 1000t입니다.");
		}
	}
}

